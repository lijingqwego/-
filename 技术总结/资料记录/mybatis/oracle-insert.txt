1.批量插入
<insert id="insertList" parameterType="java.util.List" useGeneratedKeys="false">
  		INSERT INTO P_UM_AUTHORITY_RESOURCE (RESOURCE_ID, AUTHORITYID, FLAGINFO)
          <foreach collection="list" item="item" index="index" separator="union all" > 
          	  (select #{item.resourceId,jdbcType=VARCHAR}, #{item.authorityid,jdbcType=VARCHAR}, #{item.flaginfo,jdbcType=VARCHAR} from dual)
         </foreach>
  </insert>
  
  
  
  <insert id="insertList" parameterType="java.util.List">
  	 	insert into T_EXA_QUESTIONS (QUESTIONS_NO, SUBJECT_NO,QUESTION_ID,QUESTIONS_NAME,QUESTIONS_DESC,CREATE_TIME, UPDATE_TIME)
  	 	 (
  		 <foreach collection="list" item="item" index="index" separator="union all" > 
          	  select #{item.questionsNo,jdbcType=VARCHAR},
          	  #{item.subjectNo,jdbcType=DECIMAL}, 
          	  #{item.questionId,jdbcType=VARCHAR},
          	  #{item.questionsName,jdbcType=VARCHAR},
          	  #{item.questionsDesc,jdbcType=VARCHAR},
          	  sysdate,sysdate from dual
         </foreach>
         )
  </insert

  
2.带序列插入

<insert id="insertSelective" parameterType="cn.com.user.pojo.TSubjectInfo">
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon Aug 14 15:21:26 CST 2017.
    -->
    <selectKey resultType="DECIMAL" keyProperty="subjectNo" order="BEFORE">
	   select seq_subject.nextVal
	   from dual
  	</selectKey>
    insert into T_SUBJECT_INFO
    <trim prefix="(" suffix=")" suffixOverrides=",">
        SUBJECT_NO,
        SUBJECT_CODE,
      <if test="subjectName != null">
        SUBJECT_NAME,
      </if>
      <if test="classContent != null">
        CLASS_CONTENT,
      </if>
      <if test="classTarget != null">
        CLASS_TARGET,
      </if>
      <if test="classHour != null">
        CLASS_HOUR,
      </if>
      <if test="subjectOrder != null">
        SUBJECT_ORDER,
      </if>
        FATHER_SUBJECT,
        CREATE_TIME,
        UPDATE_TIME,
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
        #{subjectNo,jdbcType=DECIMAL},
        #{subjectCode,jdbcType=VARCHAR},
      <if test="subjectName != null">
        #{subjectName,jdbcType=VARCHAR},
      </if>
      <if test="classContent != null">
        #{classContent,jdbcType=VARCHAR},
      </if>
      <if test="classTarget != null">
        #{classTarget,jdbcType=VARCHAR},
      </if>
      <if test="classHour != null">
        #{classHour,jdbcType=DECIMAL},
      </if>
       <if test="subjectOrder != null">
       #{subjectOrder,jdbcType=VARCHAR},
      </if>
        #{fatherSubject,jdbcType=DECIMAL},
        sysdate,
        sysdate,
    </trim>
  </insert>
  
3.使用序列批量插入
    <insert id="inserts" parameterType="java.util.List">
       insert into PRESON（all）
        select SEQ_PRESON_ID.NEXTVAL,A.* from(
        <foreach collection="list" item="item" index="index"
            separator="UNION">
            SELECT
            #{item.presonName},
            #{item.presonTel},
            #{item.presonEmail},
            #{item.presonAge}
            from dual 
             </foreach>
        ) A

    </insert>